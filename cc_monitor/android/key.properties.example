# Release 签名配置示例
# 复制此文件为 key.properties 并填写实际值
# 注意：key.properties 不应提交到版本控制
#
# 签名配置优先级：
# 1. 环境变量（CI/CD workflow secrets）
# 2. key.properties 文件（本地开发）
# 3. debug 签名（fallback）
#
# GitHub Actions Secrets 配置：
#   KEYSTORE_BASE64   - keystore 文件的 base64 编码
#   KEYSTORE_PASSWORD - storePassword
#   KEY_ALIAS         - keyAlias (默认: release)
#   KEY_PASSWORD      - keyPassword

# 密钥库文件路径（相对于 android/app 目录）
storeFile=release.keystore

# 密钥库密码（对应 secrets.KEYSTORE_PASSWORD）
storePassword=your_store_password

# 密钥别名（对应 secrets.KEY_ALIAS，默认 release）
keyAlias=release

# 密钥密码（对应 secrets.KEY_PASSWORD）
keyPassword=your_key_password
